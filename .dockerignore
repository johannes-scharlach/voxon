# Context for client/Dockerfile (repo root). The proxy image uses ./proxy
# as its context and has its own .dockerignore.
node_modules
**/node_modules
**/dist
client/dist
proxy/_build
proxy/deps
.git
